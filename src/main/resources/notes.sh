# to initialize the project I ran the following command
# there seems to be a bug where if you specify the archetype version it won't work, and say it doesn't exist (2/4/2020)
mvn archetype:generate -DgroupId=com.gabb.sb -DartifactId=websocket-sb -DarchetypeArtifactId=maven-archetype-quickstart  -DinteractiveMode=false